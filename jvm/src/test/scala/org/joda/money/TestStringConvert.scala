package org.joda.money

import org.testng.Assert.assertEquals
//import org.joda.convert.StringConvert
import org.testng.annotations.Test
//remove if not needed
import scala.collection.JavaConversions._

/**
 * Test string conversion.
 */
@Test
class TestStringConvert {
/*
  def test_BigMoney() {
    val test = BigMoney.of(CurrencyUnit.CHF, 1234.5678d)
    val str = StringConvert.INSTANCE.convertToString(test)
    assertEquals("CHF 1234.5678", str)
    assertEquals(test, StringConvert.INSTANCE.convertFromString(classOf[BigMoney], str))
  }

  def test_Money() {
    val test = Money.of(CurrencyUnit.CHF, 1234.56d)
    val str = StringConvert.INSTANCE.convertToString(test)
    assertEquals("CHF 1234.56", str)
    assertEquals(test, StringConvert.INSTANCE.convertFromString(classOf[Money], str))
  }

  def test_CurrencyUnit() {
    val test = CurrencyUnit.CHF
    val str = StringConvert.INSTANCE.convertToString(test)
    assertEquals("CHF", str)
    assertEquals(test, StringConvert.INSTANCE.convertFromString(classOf[CurrencyUnit], str))
  }
*/
}
